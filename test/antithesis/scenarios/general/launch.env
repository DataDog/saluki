# Per-scenario launch settings for test/antithesis/bin/launch.sh.
SCENARIO_TEST_NAME="saluki-adp-general"
SCENARIO_DESCRIPTION="ADP general: converged Datadog Agent with the data plane on, feral DogStatsD"
# The single SUT container. intake (assertions) and workload (driver) are not faulted.
SCENARIO_FAULT_NODES="agent"
