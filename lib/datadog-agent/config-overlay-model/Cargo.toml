[package]
name = "datadog-agent-config-overlay-model"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

# Maintain this crate as a leaf node and do not depend on other workspace crates. build.rs files
# depend on this and keeping it as a leaf node simplifies the dependency mental model.
[dependencies]
indexmap = { workspace = true, features = ["serde"] }
serde = { workspace = true }
serde_yaml = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
