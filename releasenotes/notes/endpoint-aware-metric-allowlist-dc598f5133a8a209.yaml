features:
  - |
    Add the experimental ``experimental.metrics_endpoint_routing.metric_allowlist`` setting
    for endpoint-aware metric routing. Selected primary or additional endpoints
    receive only exact-name allowlisted metrics, including sketches. Endpoints absent
    from the policy retain their ordinary behavior.
