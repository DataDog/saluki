import{_ as e,o as d,c as o,a2 as a}from"./chunks/framework.Dc_BP1_D.js";const p=JSON.parse('{"title":"Configuring DogStatsD on Agent Data Plane","description":"","frontmatter":{},"headers":[],"relativePath":"agent-data-plane/configuration/dogstatsd.md","filePath":"agent-data-plane/configuration/dogstatsd.md","lastUpdated":1780502171000}'),r={name:"agent-data-plane/configuration/dogstatsd.md"};function s(i,t,n,c,l,g){return d(),o("div",null,[...t[0]||(t[0]=[a('<h1 id="configuring-dogstatsd-on-agent-data-plane" tabindex="-1">Configuring DogStatsD on Agent Data Plane <a class="header-anchor" href="#configuring-dogstatsd-on-agent-data-plane" aria-label="Permalink to &quot;Configuring DogStatsD on Agent Data Plane&quot;">​</a></h1><p>The DogStatsD implementation on ADP has been redesigned in Rust for better resource guarantees and efficiency. Because the architecture is different from the original implementation, certain configuration values may behave differently, be planned but not yet implemented, or not apply at all. This page documents those nuances.</p><p>ADP is designed to be transparent: customers configure DogStatsD the same way they always have. The sections below call out the cases where that is either not yet true, or not quite possible.</p><p>If you find an error on this page, please <a href="https://github.com/DataDog/saluki/issues" target="_blank" rel="noreferrer">open an issue</a>.</p><h2 id="unsupported-settings" tabindex="-1">Unsupported Settings <a class="header-anchor" href="#unsupported-settings" aria-label="Permalink to &quot;Unsupported Settings&quot;">​</a></h2><h3 id="being-worked-on" tabindex="-1">Being Worked On <a class="header-anchor" href="#being-worked-on" aria-label="Permalink to &quot;Being Worked On&quot;">​</a></h3><p>The following settings are not yet supported in ADP but are planned with GitHub issue links for tracking.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th><th>Issue</th></tr></thead><tbody><tr><td><code>dogstatsd_capture_depth</code></td><td>Traffic capture channel depth</td><td><a href="https://github.com/DataDog/saluki/issues/1381" target="_blank" rel="noreferrer">#1381</a></td></tr><tr><td><code>dogstatsd_capture_path</code></td><td>Traffic capture file location</td><td><a href="https://github.com/DataDog/saluki/issues/1381" target="_blank" rel="noreferrer">#1381</a></td></tr><tr><td><code>dogstatsd_pipe_name</code></td><td>Windows named pipe path</td><td><a href="https://github.com/DataDog/saluki/issues/1466" target="_blank" rel="noreferrer">#1466</a></td></tr><tr><td><code>dogstatsd_windows_pipe_security_descriptor</code></td><td>Windows named pipe ACL descriptor</td><td><a href="https://github.com/DataDog/saluki/issues/1466" target="_blank" rel="noreferrer">#1466</a></td></tr><tr><td><code>forwarder_http_protocol</code></td><td>HTTP version (auto/http1)</td><td><a href="https://github.com/DataDog/saluki/issues/1361" target="_blank" rel="noreferrer">#1361</a></td></tr><tr><td><code>forwarder_outdated_file_in_days</code></td><td>Retry file retention (days)</td><td><a href="https://github.com/DataDog/saluki/issues/1360" target="_blank" rel="noreferrer">#1360</a></td></tr><tr><td><code>serializer_experimental_use_v3_api.*</code></td><td>V3 metrics API migration flags</td><td><a href="https://github.com/DataDog/saluki/issues/1468" target="_blank" rel="noreferrer">#1468</a></td></tr><tr><td><code>sslkeylogfile</code></td><td>TLS key log file path</td><td><a href="https://github.com/DataDog/saluki/issues/1372" target="_blank" rel="noreferrer">#1372</a></td></tr><tr><td><code>tls_handshake_timeout</code></td><td>HTTP TLS handshake timeout</td><td><a href="https://github.com/DataDog/saluki/issues/178" target="_blank" rel="noreferrer">#178</a></td></tr></tbody></table><h3 id="not-planned" tabindex="-1">Not Planned <a class="header-anchor" href="#not-planned" aria-label="Permalink to &quot;Not Planned&quot;">​</a></h3><p>The following settings exist in the core agent but are not planned for ADP, typically because ADP&#39;s architecture is fundamentally different or the feature is platform-specific.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th><th>Reason</th></tr></thead><tbody><tr><td><code>config_id</code></td><td>Fleet Automation config ID tag</td><td>Core Agent uses this only on Agent HA telemetry metrics</td></tr><tr><td><code>dogstatsd_host_socket_path</code></td><td>Host UDS socket dir for DSD</td><td>Not read by DSD server; admission controller only</td></tr><tr><td><code>dogstatsd_mem_based_rate_limiter.*</code></td><td>Memory-based rate limiter</td><td>Go GC–specific; ADP uses <code>memory_limit</code> (see below)</td></tr><tr><td><code>dogstatsd_no_aggregation_pipeline_batch_size</code></td><td>No-aggregation pipeline batch size</td><td>Fixed in ADP topology</td></tr><tr><td><code>dogstatsd_packet_buffer_flush_timeout</code></td><td>Packet buffer flush timeout</td><td>ADP decodes inline</td></tr><tr><td><code>dogstatsd_packet_buffer_size</code></td><td>Datagrams per packet buffer</td><td>ADP decodes inline</td></tr><tr><td><code>dogstatsd_pipeline_autoadjust</code></td><td>Auto-adjust pipeline workers</td><td>ADP uses async tasks</td></tr><tr><td><code>dogstatsd_pipeline_count</code></td><td>Parallel processing pipelines</td><td>ADP uses async tasks</td></tr><tr><td><code>dogstatsd_queue_size</code></td><td>Packet channel buffer size</td><td>ADP uses async tasks</td></tr><tr><td><code>dogstatsd_telemetry_enabled_listener_id</code></td><td>Per-listener telemetry tagging</td><td>Not feasible to thread through</td></tr><tr><td><code>dogstatsd_workers_count</code></td><td>Number of DSD processing workers</td><td>ADP uses async tasks</td></tr><tr><td><code>entity_id</code></td><td>Agent pod entity ID</td><td>ADP internal DogStatsD telemetry uses OpenMetrics</td></tr><tr><td><code>use_dogstatsd</code></td><td>Master DogStatsD enable toggle</td><td>Core Agent evaluates and sets <code>data_plane.dogstatsd.enabled</code></td></tr></tbody></table><h3 id="memory-based-rate-limiter-dogstatsd-mem-based-rate-limiter" tabindex="-1">Memory-based rate limiter (<code>dogstatsd_mem_based_rate_limiter.*</code>) <a class="header-anchor" href="#memory-based-rate-limiter-dogstatsd-mem-based-rate-limiter" aria-label="Permalink to &quot;Memory-based rate limiter (`dogstatsd_mem_based_rate_limiter.*`)&quot;">​</a></h3><p>The Core Agent exposes configuration under this prefix to apply backpressure when the Go process approaches its memory limit. They work by manipulating Go&#39;s garbage collector (<code>debug.SetGCPercent</code>, <code>debug.FreeOSMemory</code>), allocating a large heap ballast to adjust GC heuristics, and blocking goroutines to slow packet ingestion. None of these mechanisms have an equivalent in Rust, and ADP does not use a Go runtime.</p><p>ADP takes a different approach to the same problem using explicit static memory accounting and a process-level RSS limit. All 11 <code>dogstatsd_mem_based_rate_limiter.*</code> keys are ignored. See <a href="./../memory">Memory Management</a> for details.</p><h2 id="behavioral-differences" tabindex="-1">Behavioral Differences <a class="header-anchor" href="#behavioral-differences" aria-label="Permalink to &quot;Behavioral Differences&quot;">​</a></h2><p>The following settings are recognized by both ADP and the core agent, but with different behavior or default values.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th><th>Agent Behavior</th><th>ADP Behavior</th></tr></thead><tbody><tr><td><code>dogstatsd_mapper_cache_size</code></td><td>Mapper result LRU cache size</td><td><code>0</code> disables mapping; positive sizes the LRU</td><td><code>0</code> disables the cache only; mapping still runs (<a href="https://github.com/DataDog/saluki/issues/1687" target="_blank" rel="noreferrer">#1687</a>)</td></tr><tr><td><code>dogstatsd_metrics_stats_enable</code></td><td>Enable per-metric debug stats</td><td>Config toggle</td><td>Gates debug log; stats API on-demand (<a href="https://github.com/DataDog/saluki/issues/1352" target="_blank" rel="noreferrer">#1352</a>, <a href="https://github.com/DataDog/saluki/issues/1356" target="_blank" rel="noreferrer">#1356</a>)</td></tr><tr><td><code>dogstatsd_stats_enable</code></td><td>Enable internal stats endpoint</td><td>Config toggle</td><td>On-demand via API (<a href="https://github.com/DataDog/saluki/issues/1352" target="_blank" rel="noreferrer">#1352</a>)</td></tr><tr><td><code>dogstatsd_stats_buffer</code></td><td>Internal stats buffer size</td><td>Configurable</td><td>On-demand via API (<a href="https://github.com/DataDog/saluki/issues/1352" target="_blank" rel="noreferrer">#1352</a>)</td></tr><tr><td><code>dogstatsd_stats_port</code></td><td>Internal stats endpoint port</td><td>Configurable port</td><td>On-demand via API (<a href="https://github.com/DataDog/saluki/issues/1352" target="_blank" rel="noreferrer">#1352</a>)</td></tr><tr><td><code>log_level</code></td><td>Log verbosity directives</td><td>Controls Agent logs</td><td>Plain levels control ADP/Saluki-owned targets only</td></tr><tr><td><code>logging_frequency</code></td><td>Transaction success log interval</td><td>Throttles success logs</td><td>Intentionally unused</td></tr><tr><td><code>min_tls_version</code></td><td>Minimum outbound TLS version</td><td>Supports TLS 1.0, 1.1, 1.2, and 1.3</td><td>Supports TLS 1.2+ and TLS 1.3-only; clamps TLS 1.0/1.1 to 1.2</td></tr><tr><td><code>serializer_zstd_compressor_level</code></td><td>Zstd compression level</td><td>Default level 1</td><td>Default level 3 (intentional)</td></tr><tr><td><code>skip_ssl_validation</code></td><td>Skip TLS cert validation</td><td>Disables validation for outbound HTTPS clients</td><td>Applies to the shared Datadog forwarder; rejected in FIPS mode</td></tr></tbody></table><h3 id="datadog-intake-tls-protocol-version-min-tls-version" tabindex="-1">Datadog intake TLS protocol version (<code>min_tls_version</code>) <a class="header-anchor" href="#datadog-intake-tls-protocol-version-min-tls-version" aria-label="Permalink to &quot;Datadog intake TLS protocol version (`min_tls_version`)&quot;">​</a></h3><p>ADP supports <code>min_tls_version</code> for Datadog intake forwarding through the shared Datadog forwarder. The default is <code>tlsv1.2</code>, which allows TLS 1.2 and TLS 1.3. To require TLS 1.3 only, set <code>min_tls_version: tlsv1.3</code> or <code>DD_MIN_TLS_VERSION=tlsv1.3</code>.</p><p>The core agent also accepts <code>tlsv1.0</code> and <code>tlsv1.1</code>. ADP accepts those values for configuration compatibility, but clamps them to TLS 1.2 because ADP uses <code>rustls</code>, which doesn&#39;t support TLS 1.0 or TLS 1.1.</p><p>This setting doesn&#39;t affect ADP IPC, local privileged APIs, ADP control-plane clients, OTLP proxying to the core agent, or unrelated HTTP clients.</p><h3 id="dogstatsd-forwarding-statsd-forward-host-statsd-forward-port" tabindex="-1">DogStatsD forwarding (<code>statsd_forward_host</code> / <code>statsd_forward_port</code>) <a class="header-anchor" href="#dogstatsd-forwarding-statsd-forward-host-statsd-forward-port" aria-label="Permalink to &quot;DogStatsD forwarding (`statsd_forward_host` / `statsd_forward_port`)&quot;">​</a></h3><p>ADP supports DogStatsD forwarding when both <code>statsd_forward_host</code> and <code>statsd_forward_port</code> are set. ADP forwards each framed DogStatsD message over UDP to the configured destination before parsing, filtering, mapping, or aggregation. Forwarding doesn&#39;t preserve the core Agent&#39;s packet-buffer grouping, so forwarded UDP datagrams may be split differently while carrying the same DogStatsD messages. ADP logs setup failures and tracks send failures through telemetry.</p><h3 id="multi-region-failover-metrics" tabindex="-1">Multi-region failover metrics <a class="header-anchor" href="#multi-region-failover-metrics" aria-label="Permalink to &quot;Multi-region failover metrics&quot;">​</a></h3><p>ADP supports multi-region failover for metrics. When <code>multi_region_failover.enabled</code> and <code>multi_region_failover.failover_metrics</code> are both <code>true</code>, ADP forwards metrics to the primary Datadog endpoint and to a second MRF endpoint.</p><p>To enable the MRF metrics branch at startup, configure <code>multi_region_failover.api_key</code> and one of <code>multi_region_failover.site</code> or <code>multi_region_failover.dd_url</code>. If the MRF endpoint configuration is incomplete, ADP skips the MRF branch and continues forwarding to the primary endpoint.</p><p>ADP does not fall back to the primary <code>api_key</code>, <code>site</code>, or <code>dd_url</code> for MRF traffic. <code>multi_region_failover.enabled</code> and endpoint selection are resolved at startup. <code>multi_region_failover.failover_metrics</code>, <code>multi_region_failover.metric_allowlist</code>, and <code>multi_region_failover.api_key</code> can refresh from live configuration after the MRF branch is enabled.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Behavior</th><th>Default</th></tr></thead><tbody><tr><td><code>multi_region_failover.enabled</code></td><td>Enables multi-region failover mode.</td><td><code>false</code></td></tr><tr><td><code>multi_region_failover.failover_metrics</code></td><td>Enables metrics forwarding to the failover region when MRF is enabled.</td><td><code>false</code></td></tr><tr><td><code>multi_region_failover.metric_allowlist</code></td><td>Exact metric names to forward to MRF. Empty or unset forwards all metrics.</td><td><code>[]</code></td></tr><tr><td><code>multi_region_failover.api_key</code></td><td>API key for the failover-region endpoint.</td><td>unset</td></tr><tr><td><code>multi_region_failover.site</code></td><td>Datadog site for the failover region, used as <code>https://app.mrf.&lt;site&gt;</code>.</td><td>unset</td></tr><tr><td><code>multi_region_failover.dd_url</code></td><td>Explicit failover intake URL. Takes precedence over <code>site</code> when set.</td><td>unset</td></tr></tbody></table><h3 id="datadog-intake-tls-validation-skip-ssl-validation" tabindex="-1">Datadog intake TLS validation (<code>skip_ssl_validation</code>) <a class="header-anchor" href="#datadog-intake-tls-validation-skip-ssl-validation" aria-label="Permalink to &quot;Datadog intake TLS validation (`skip_ssl_validation`)&quot;">​</a></h3><p>ADP supports <code>skip_ssl_validation</code> for Datadog intake forwarding through the shared Datadog forwarder. The default is <code>false</code>, which preserves normal server certificate validation. To accept invalid server certificates for Datadog intake requests, set <code>skip_ssl_validation: true</code> or <code>DD_SKIP_SSL_VALIDATION=true</code>.</p><p>When enabled, this setting affects the Datadog intake clients used by metrics, logs, traces, events, and service checks that flow through the shared forwarder.</p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p>Setting <code>skip_ssl_validation: true</code> disables TLS server certificate validation for Datadog intake forwarding. Use it only when you understand and accept that risk.</p></div><p>This setting does not affect ADP IPC, local privileged APIs, ADP control-plane clients, OTLP proxying to the core agent, or unrelated HTTP clients. In FIPS builds, ADP rejects <code>skip_ssl_validation: true</code> because disabling TLS certificate validation is not FIPS-compliant.</p><h3 id="logging-verbosity-log-level-logging-frequency" tabindex="-1">Logging verbosity (<code>log_level</code> / <code>logging_frequency</code>) <a class="header-anchor" href="#logging-verbosity-log-level-logging-frequency" aria-label="Permalink to &quot;Logging verbosity (`log_level` / `logging_frequency`)&quot;">​</a></h3><p>ADP accepts <code>log_level</code> as the startup logging control. A plain level applies to ADP-owned and Saluki-owned targets only, including <code>agent_data_plane</code>, <code>saluki_*</code>, and runtime crates under <code>lib/</code>.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">log_level</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">debug</span></span></code></pre></div><p>This keeps third-party dependencies such as <code>hyper</code>, <code>tokio</code>, and <code>tonic</code> at their default filtering unless you opt them in.</p><p>To control dependency logs or set a global fallback, use advanced <code>EnvFilter</code> directives in <code>log_level</code>. ADP applies those directive strings as configured:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">log_level</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">warn,agent_data_plane=debug,hyper=warn</span></span></code></pre></div><p><code>logging_frequency</code> is intentionally unused by ADP. The core agent uses it to throttle repetitive successful transaction logs. ADP logs successful forwarder operations below the default <code>info</code> level, so there is no matching info-level success-log stream to throttle.</p><h3 id="dogstatsd-statistics-dogstatsd-stats-enable-dogstatsd-metrics-stats-enable" tabindex="-1">DogStatsD statistics (<code>dogstatsd_stats_enable</code> / <code>dogstatsd_metrics_stats_enable</code>) <a class="header-anchor" href="#dogstatsd-statistics-dogstatsd-stats-enable-dogstatsd-metrics-stats-enable" aria-label="Permalink to &quot;DogStatsD statistics (`dogstatsd_stats_enable` / `dogstatsd_metrics_stats_enable`)&quot;">​</a></h3><p>The core agent has two DogStatsD statistics mechanisms with different scopes. <code>dogstatsd_stats_enable</code> enables packet-level throughput statistics from a ring buffer, exposed as Go expvar data on <code>dogstatsd_stats_port</code> (default <code>5000</code>). Operators must configure an OpenMetrics check to scrape that endpoint before the data is submitted. <code>dogstatsd_metrics_stats_enable</code> enables runtime-toggleable metric-level debug statistics that track count and last-seen time per unique metric and tag combination. That data powers the core agent&#39;s <code>dogstatsd-stats</code> CLI command and HTTP endpoint.</p><p>ADP does not mirror the packet-level statistics config path. Instead, ADP provides an on-demand metric-level view through a DogStatsD statistics destination that is always wired into the topology, but only collects data during a time-bounded request. To collect statistics, run <code>agent-data-plane dogstatsd stats --duration-secs N</code> or call the privileged <code>/dogstatsd/stats?collection_duration_secs=N</code> API. The handler waits for the requested collection window, then returns count and last-seen time per metric context inline as JSON. The CLI uses the same API and renders the result as either summary or cardinality analysis.</p><p>ADP also exposes internal DogStatsD telemetry through its OpenMetrics endpoint, always-on at <code>http://&lt;api_listen_address&gt;/metrics</code> (the unprivileged API endpoint; default port <code>5100</code>). Scrape that endpoint to collect aggregate DogStatsD counters such as processed message counts, packet and byte counts, packet pool usage, and channel latency. This endpoint is separate from <code>/dogstatsd/stats</code>: it does not return the per-metric count and last-seen map, and it is not controlled by the core agent&#39;s <code>dogstatsd_stats_*</code> keys.</p><p>ADP does not expose the core agent&#39;s packet-per-second expvar endpoint or a persistent per-metric DogStatsD statistics endpoint to scrape. You do not need to set up scraper configuration for this per-metric data. The config keys <code>dogstatsd_stats_enable</code>, <code>dogstatsd_stats_buffer</code>, and <code>dogstatsd_stats_port</code> have no effect in ADP. See <a href="https://github.com/DataDog/saluki/issues/1352" target="_blank" rel="noreferrer">#1352</a>.</p><h3 id="dogstatsd-metric-debug-log" tabindex="-1">DogStatsD metric debug log <a class="header-anchor" href="#dogstatsd-metric-debug-log" aria-label="Permalink to &quot;DogStatsD metric debug log&quot;">​</a></h3><p>ADP supports the core agent&#39;s DogStatsD metric debug log. To write this file, set <code>dogstatsd_metrics_stats_enable: true</code>. <code>dogstatsd_logging_enabled</code> also must be <code>true</code>; it defaults to <code>true</code>, so most configurations only need to enable <code>dogstatsd_metrics_stats_enable</code>.</p><p>When <code>dogstatsd_logging_enabled</code> is <code>true</code>, ADP connects an extra DogStatsD destination to the decoded metric stream. The destination writes one line per metric sample with the metric name, tags, count, and last-seen time while <code>dogstatsd_metrics_stats_enable</code> is <code>true</code>. When <code>dogstatsd_metrics_stats_enable</code> is <code>false</code>, the destination drains decoded metrics and drops them. This lets runtime configuration changes start and stop the debug log without rebuilding the topology. This feature is for support and troubleshooting. It does not change normal metric forwarding, and it does not replace the on-demand <code>/dogstatsd/stats</code> API.</p><p>Use these settings to control the file:</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Behavior</th></tr></thead><tbody><tr><td><code>dogstatsd_log_file</code></td><td>Output path. If empty, ADP uses the platform default DogStatsD stats log path.</td></tr><tr><td><code>dogstatsd_log_file_max_rolls</code></td><td>Number of rotated files to keep. Defaults to <code>3</code>.</td></tr><tr><td><code>dogstatsd_log_file_max_size</code></td><td>Maximum active file size before rotation. Defaults to <code>10Mb</code>.</td></tr><tr><td><code>dogstatsd_logging_enabled</code></td><td>Controls whether ADP wires the debug log destination into the topology. Defaults to <code>true</code>.</td></tr></tbody></table><p>The default <code>dogstatsd_log_file</code> path is <code>/var/log/datadog/dogstatsd_info/dogstatsd-stats.log</code> on Linux and other Unix platforms, <code>/opt/datadog-agent/logs/dogstatsd_info/dogstatsd-stats.log</code> on macOS, and <code>%ProgramData%\\datadog\\logs\\dogstatsd_info\\dogstatsd-stats.log</code> on Windows.</p><p>This debug log differs from the <code>dogstatsd_capture_*</code> settings. The debug log records decoded metric summaries after DogStatsD parsing. The capture settings record raw DogStatsD traffic for packet-level investigation, and they remain tracked separately under <a href="https://github.com/DataDog/saluki/issues/1381" target="_blank" rel="noreferrer">#1381</a>.</p><h3 id="payload-debug-logging-log-payloads" tabindex="-1">Payload debug logging (<code>log_payloads</code>) <a class="header-anchor" href="#payload-debug-logging-log-payloads" aria-label="Permalink to &quot;Payload debug logging (`log_payloads`)&quot;">​</a></h3><p>ADP supports <code>log_payloads</code> for debugging metric, event, and service check payload contents before they enter Datadog encoders. To see these logs, set <code>log_payloads: true</code> and run with debug-level logging enabled.</p><p>When enabled, ADP logs decoded payload objects: scalar series metrics, sketches/distributions, events, and service checks. These logs can contain high-volume customer data, including metric names, tags, host and container metadata, event text, and service check messages. Use this setting only while diagnosing payload content.</p><p>ADP does not dump the exact encoded JSON or protobuf HTTP request body, and it does not log compressed wire payload bytes.</p><h3 id="dogstatsd-mapper-cache-size" tabindex="-1"><code>dogstatsd_mapper_cache_size</code> <a class="header-anchor" href="#dogstatsd-mapper-cache-size" aria-label="Permalink to &quot;`dogstatsd_mapper_cache_size`&quot;">​</a></h3><p>ADP and the core agent both cache mapper results to skip regex evaluation on repeat metric names. With the default value of <code>1000</code>, and with any positive integer, behavior matches the core agent: results are cached in an LRU keyed by the original metric name, including a negative-cache entry for names that match no profile.</p><p>The two implementations diverge when this setting is <code>0</code>. In the core agent, <code>0</code> is rejected by the underlying LRU library, which causes the entire mapper to be silently disabled: mapping profiles configured by <code>dogstatsd_mapper_profiles</code> are not applied. In ADP, <code>0</code> disables the result cache only; mapping profiles still run, so each metric pays the regex evaluation cost without amortization.</p><p>If you previously set <code>dogstatsd_mapper_cache_size: 0</code> in the core agent to turn off the mapper, clear <code>dogstatsd_mapper_profiles</code> instead when running ADP. See <a href="https://github.com/DataDog/saluki/issues/1687" target="_blank" rel="noreferrer">#1687</a>.</p><h2 id="compatibility-unknown" tabindex="-1">Compatibility Unknown <a class="header-anchor" href="#compatibility-unknown" aria-label="Permalink to &quot;Compatibility Unknown&quot;">​</a></h2><p>The following settings need further investigation. ADP behavior may differ from the core agent in ways that are not yet fully characterized.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th><th>Issue</th></tr></thead><tbody><tr><td><code>aggregator_buffer_size</code></td><td>Channel buffer depth for aggregator queues</td><td><a href="https://github.com/DataDog/saluki/issues/1681" target="_blank" rel="noreferrer">#1681</a></td></tr><tr><td><code>aggregator_flush_metrics_and_serialize_in_parallel_buffer_size</code></td><td>Parallel flush: series/sketch buffer size</td><td><a href="https://github.com/DataDog/saluki/issues/1681" target="_blank" rel="noreferrer">#1681</a></td></tr><tr><td><code>aggregator_flush_metrics_and_serialize_in_parallel_chan_size</code></td><td>Parallel flush: channel size</td><td><a href="https://github.com/DataDog/saluki/issues/1681" target="_blank" rel="noreferrer">#1681</a></td></tr><tr><td><code>aggregator_stop_timeout</code></td><td>Timeout (s) for aggregator flush on stop</td><td><a href="https://github.com/DataDog/saluki/issues/1681" target="_blank" rel="noreferrer">#1681</a></td></tr><tr><td><code>aggregator_use_tags_store</code></td><td>Enable shared tag deduplication store</td><td><a href="https://github.com/DataDog/saluki/issues/1681" target="_blank" rel="noreferrer">#1681</a></td></tr><tr><td><code>anomaly_detection.enabled</code></td><td>Enable anomaly detection observer pipeline</td><td><a href="https://github.com/DataDog/saluki/issues/1683" target="_blank" rel="noreferrer">#1683</a></td></tr><tr><td><code>anomaly_detection.metrics.enabled</code></td><td>Enable metric ingestion for anomaly detection</td><td><a href="https://github.com/DataDog/saluki/issues/1683" target="_blank" rel="noreferrer">#1683</a></td></tr><tr><td><code>autoscaling.failover.enabled</code></td><td>Enable autoscaling failover metric routing</td><td><a href="https://github.com/DataDog/saluki/issues/1684" target="_blank" rel="noreferrer">#1684</a></td></tr><tr><td><code>autoscaling.failover.metrics</code></td><td>Metric names forwarded to DCA for failover</td><td><a href="https://github.com/DataDog/saluki/issues/1684" target="_blank" rel="noreferrer">#1684</a></td></tr><tr><td><code>dogstatsd_disable_verbose_logs</code></td><td>Suppress noisy parse error logs</td><td><a href="https://github.com/DataDog/saluki/issues/1350" target="_blank" rel="noreferrer">#1350</a></td></tr><tr><td><code>dogstatsd_experimental_http.enabled</code></td><td>Enable experimental HTTP/H2C DSD listener</td><td><a href="https://github.com/DataDog/saluki/issues/1682" target="_blank" rel="noreferrer">#1682</a></td></tr><tr><td><code>dogstatsd_experimental_http.listen_address</code></td><td>Bind address for experimental HTTP DSD listener</td><td><a href="https://github.com/DataDog/saluki/issues/1682" target="_blank" rel="noreferrer">#1682</a></td></tr><tr><td><code>enable_json_stream_shared_compressor_buffers</code></td><td>Pre-allocate shared compressor buffers</td><td><a href="https://github.com/DataDog/saluki/issues/1749" target="_blank" rel="noreferrer">#1749</a></td></tr><tr><td><code>forwarder_apikey_validation_interval</code></td><td>API key check interval (minutes)</td><td><a href="https://github.com/DataDog/saluki/issues/1357" target="_blank" rel="noreferrer">#1357</a></td></tr><tr><td><code>forwarder_flush_to_disk_mem_ratio</code></td><td>Mem-to-disk flush threshold</td><td><a href="https://github.com/DataDog/saluki/issues/1364" target="_blank" rel="noreferrer">#1364</a></td></tr><tr><td><code>forwarder_high_prio_buffer_size</code></td><td>High-priority request queue size</td><td><a href="https://github.com/DataDog/saluki/issues/1362" target="_blank" rel="noreferrer">#1362</a></td></tr><tr><td><code>forwarder_low_prio_buffer_size</code></td><td>Low-priority request queue size</td><td><a href="https://github.com/DataDog/saluki/issues/1362" target="_blank" rel="noreferrer">#1362</a></td></tr><tr><td><code>forwarder_max_concurrent_requests</code></td><td>Max concurrent HTTP requests</td><td><a href="https://github.com/DataDog/saluki/issues/1363" target="_blank" rel="noreferrer">#1363</a></td></tr><tr><td><code>forwarder_requeue_buffer_size</code></td><td>In-memory re-queue buffer size</td><td><a href="https://github.com/DataDog/saluki/issues/1755" target="_blank" rel="noreferrer">#1755</a></td></tr><tr><td><code>forwarder_retry_queue_capacity_time_interval_sec</code></td><td>Retry queue time-based capacity</td><td><a href="https://github.com/DataDog/saluki/issues/1365" target="_blank" rel="noreferrer">#1365</a></td></tr><tr><td><code>forwarder_stop_timeout</code></td><td>Timeout (s) for forwarder graceful stop</td><td>[#1680]</td></tr><tr><td><code>heroku_dyno</code></td><td>Override agent name for Heroku telemetry</td><td>[#1685]</td></tr><tr><td><code>telemetry.dogstatsd.aggregator_channel_latency_buckets</code></td><td>Histogram buckets: DSD aggregator channel lag</td><td><a href="https://github.com/DataDog/saluki/issues/1679" target="_blank" rel="noreferrer">#1679</a></td></tr><tr><td><code>telemetry.dogstatsd.listeners_channel_latency_buckets</code></td><td>Histogram buckets: listener channel latency</td><td><a href="https://github.com/DataDog/saluki/issues/1679" target="_blank" rel="noreferrer">#1679</a></td></tr><tr><td><code>telemetry.dogstatsd.listeners_latency_buckets</code></td><td>Histogram buckets: listener processing</td><td><a href="https://github.com/DataDog/saluki/issues/1679" target="_blank" rel="noreferrer">#1679</a></td></tr><tr><td><code>telemetry.dogstatsd_origin</code></td><td>Per-origin processed-metrics telemetry</td><td><a href="https://github.com/DataDog/saluki/issues/1679" target="_blank" rel="noreferrer">#1679</a></td></tr></tbody></table><h2 id="adp-only-settings" tabindex="-1">ADP-Only Settings <a class="header-anchor" href="#adp-only-settings" aria-label="Permalink to &quot;ADP-Only Settings&quot;">​</a></h2><p>The following settings are specific to ADP and have no equivalent in the core agent.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>agent_ipc_endpoint</code></td><td>Remote agent IPC URI</td><td></td></tr><tr><td><code>aggregate_flush_interval</code></td><td>Aggregator flush period</td><td></td></tr><tr><td><code>aggregate_flush_open_windows</code></td><td>Flush open windows on stop</td><td></td></tr><tr><td><code>aggregate_passthrough_idle_flush_timeout</code></td><td>Passthrough buffer flush delay</td><td></td></tr><tr><td><code>aggregate_window_duration_seconds</code></td><td>Aggregation window size</td><td></td></tr><tr><td><code>connect_retry_attempts</code></td><td>IPC client connect retries</td><td></td></tr><tr><td><code>connect_retry_backoff</code></td><td>IPC client retry delay</td><td></td></tr><tr><td><code>counter_expiry_seconds</code></td><td>Idle counter keep-alive duration</td><td>300</td></tr><tr><td><code>data_plane.api_listen_address</code></td><td>ADP unprivileged API address</td><td></td></tr><tr><td><code>data_plane.dogstatsd.aggregator_tag_filter_cache_capacity</code></td><td>Tag-filter deduplication cache size</td><td>100000</td></tr><tr><td><code>data_plane.remote_agent_enabled</code></td><td>Register as remote agent</td><td></td></tr><tr><td><code>data_plane.secure_api_listen_address</code></td><td>ADP privileged API address</td><td></td></tr><tr><td><code>data_plane.standalone_mode</code></td><td>ADP standalone mode toggle</td><td></td></tr><tr><td><code>data_plane.use_new_config_stream_endpoint</code></td><td>Use new config stream endpoint</td><td></td></tr><tr><td><code>dogstatsd_allow_context_heap_allocs</code></td><td>Allow heap allocations for contexts</td><td></td></tr><tr><td><code>dogstatsd_autoscale_udp_listeners</code></td><td>Bind multiple UDP sockets via SO_REUSEPORT</td><td></td></tr><tr><td><code>dogstatsd_buffer_count</code></td><td>Number of receive buffers</td><td></td></tr><tr><td><code>dogstatsd_cached_contexts_limit</code></td><td>Max cached metric contexts</td><td></td></tr><tr><td><code>dogstatsd_cached_tagsets_limit</code></td><td>Max cached tagsets</td><td></td></tr><tr><td><code>dogstatsd_mapper_string_interner_size</code></td><td>Mapper string interner capacity</td><td></td></tr><tr><td><code>dogstatsd_minimum_sample_rate</code></td><td>Floor for metric sample rates</td><td></td></tr><tr><td><code>dogstatsd_permissive_decoding</code></td><td>Relaxes decoder strictness</td><td>true</td></tr><tr><td><code>dogstatsd_string_interner_size_bytes</code></td><td>Explicit byte budget for context interner</td><td></td></tr><tr><td><code>dogstatsd_tcp_port</code></td><td>TCP listen port for DSD</td><td></td></tr><tr><td><code>enable_global_limiter</code></td><td>Toggle global memory limiter</td><td></td></tr><tr><td><code>flush_timeout_secs</code></td><td>Encoder flush timeout (secs)</td><td></td></tr><tr><td><code>memory_limit</code></td><td>Process memory limit (bytes)</td><td></td></tr><tr><td><code>memory_mode</code></td><td>ADP global memory limiter mode</td><td></td></tr><tr><td><code>memory_slop_factor</code></td><td>Memory headroom fraction</td><td></td></tr><tr><td><code>metrics_level</code></td><td>ADP internal metrics emission level</td><td></td></tr><tr><td><code>otlp_string_interner_size</code></td><td>OTLP context interner capacity</td><td></td></tr><tr><td><code>remote_agent_string_interner_size_bytes</code></td><td>Tag string interner capacity</td><td>512 KB</td></tr><tr><td><code>serializer_max_metrics_per_payload</code></td><td>Max metrics per payload</td><td></td></tr><tr><td><code>statsd_metric_namespace_blocklist</code></td><td>Renamed alias for blacklist key</td><td></td></tr></tbody></table><h3 id="memory-limit-memory-slop-factor" tabindex="-1"><code>memory_limit</code> / <code>memory_slop_factor</code> <a class="header-anchor" href="#memory-limit-memory-slop-factor" aria-label="Permalink to &quot;`memory_limit` / `memory_slop_factor`&quot;">​</a></h3><p>ADP uses an explicit process memory limit (<code>memory_limit</code>) rather than relying on Go&#39;s garbage collector. The <code>memory_slop_factor</code> reserves a fraction of the limit to account for allocations not tracked by ADP&#39;s internal accounting. When memory usage approaches <code>memory_limit</code>, ADP&#39;s global limiter begins exerting backpressure (see <code>enable_global_limiter</code>).</p><h3 id="dogstatsd-minimum-sample-rate" tabindex="-1"><code>dogstatsd_minimum_sample_rate</code> <a class="header-anchor" href="#dogstatsd-minimum-sample-rate" aria-label="Permalink to &quot;`dogstatsd_minimum_sample_rate`&quot;">​</a></h3><p>ADP enforces a minimum sample rate on incoming metrics to prevent memory exhaustion from extremely low sample rates on histograms and sketches. Sending metrics with a very high inverse sample rate (for example <code>@0.0000001</code>) can cause unbounded memory growth in a sketch; this setting prevents that. The default is conservative enough that normal clients are unaffected.</p><h3 id="dogstatsd-permissive-decoding" tabindex="-1"><code>dogstatsd_permissive_decoding</code> <a class="header-anchor" href="#dogstatsd-permissive-decoding" aria-label="Permalink to &quot;`dogstatsd_permissive_decoding`&quot;">​</a></h3><p>By default, ADP parses DogStatsD packets with the same leniency as the core agent, accepting packets that technically violate the spec. Setting this to <code>false</code> enables strict mode, which rejects non-conformant packets. Strict mode is not available in the core agent.</p><h3 id="data-plane-remote-agent-enabled-data-plane-use-new-config-stream-endpoint" tabindex="-1"><code>data_plane.remote_agent_enabled</code> / <code>data_plane.use_new_config_stream_endpoint</code> <a class="header-anchor" href="#data-plane-remote-agent-enabled-data-plane-use-new-config-stream-endpoint" aria-label="Permalink to &quot;`data_plane.remote_agent_enabled` / `data_plane.use_new_config_stream_endpoint`&quot;">​</a></h3><p>These two keys are transitional flags being phased out. Both will be implied by <code>data_plane.standalone_mode=false</code> in a future release. Do not rely on them for new deployments.</p><h2 id="transparent-settings" tabindex="-1">Transparent Settings <a class="header-anchor" href="#transparent-settings" aria-label="Permalink to &quot;Transparent Settings&quot;">​</a></h2><p>The following settings work in ADP with the same behavior as the core agent.</p><p>To enable syslog logging, set <code>log_to_syslog: true</code>. Console logging remains controlled by <code>log_to_console</code>; enabling syslog does not disable console or file logging. If <code>syslog_uri</code> is empty while syslog logging is enabled, ADP uses the platform default local syslog socket: <code>unixgram:///dev/log</code> on Linux and <code>unixgram:///var/run/syslog</code> on macOS. Set <code>syslog_rfc: true</code> when the receiving syslog daemon expects the Agent&#39;s RFC-style header.</p><table tabindex="0"><thead><tr><th>Config Key</th><th>Description</th></tr></thead><tbody><tr><td><code>additional_endpoints</code></td><td>Dual-ship to extra endpoints</td></tr><tr><td><code>aggregate_context_limit</code></td><td>Max contexts per aggregation window</td></tr><tr><td><code>api_key</code></td><td>API key for endpoint auth</td></tr><tr><td><code>auth_token_file_path</code></td><td>IPC auth token file path</td></tr><tr><td><code>bind_host</code></td><td>Global listen host fallback</td></tr><tr><td><code>cmd_port</code></td><td>Agent IPC/CMD API port</td></tr><tr><td><code>container_cgroup_root</code></td><td>Cgroup filesystem root path</td></tr><tr><td><code>container_proc_root</code></td><td>Procfs root path for containers</td></tr><tr><td><code>cri_socket_path</code></td><td>CRI/containerd socket path</td></tr><tr><td><code>data_plane.dogstatsd.enabled</code></td><td>Enable DSD in data plane</td></tr><tr><td><code>data_plane.enabled</code></td><td>Enable ADP globally</td></tr><tr><td><code>dd_url</code></td><td>Override intake endpoint URL</td></tr><tr><td><code>dogstatsd_buffer_size</code></td><td>Receive buffer size (bytes)</td></tr><tr><td><code>dogstatsd_context_expiry_seconds</code></td><td>Context cache TTL (seconds)</td></tr><tr><td><code>dogstatsd_entity_id_precedence</code></td><td>Entity ID over auto-detection</td></tr><tr><td><code>dogstatsd_eol_required</code></td><td>Require newline-terminated messages</td></tr><tr><td><code>dogstatsd_expiry_seconds</code></td><td>Counter zero-value TTL (secs)</td></tr><tr><td><code>dogstatsd_flush_incomplete_buckets</code></td><td>Flush open buckets on shutdown</td></tr><tr><td><code>dogstatsd_log_file</code></td><td>DSD metric debug log path</td></tr><tr><td><code>dogstatsd_log_file_max_rolls</code></td><td>Max rotated DSD debug log files</td></tr><tr><td><code>dogstatsd_log_file_max_size</code></td><td>Max DSD debug log file size</td></tr><tr><td><code>dogstatsd_logging_enabled</code></td><td>Enable DSD metric debug logging</td></tr><tr><td><code>dogstatsd_mapper_profiles</code></td><td>Metric mapping profile definitions</td></tr><tr><td><code>dogstatsd_no_aggregation_pipeline</code></td><td>Enable no-aggregation timestamped path</td></tr><tr><td><code>dogstatsd_non_local_traffic</code></td><td>Accept non-localhost UDP/TCP</td></tr><tr><td><code>dogstatsd_origin_detection</code></td><td>Enable UDS origin detection</td></tr><tr><td><code>dogstatsd_origin_detection_client</code></td><td>Honor client origin proto fields</td></tr><tr><td><code>dogstatsd_origin_optout_enabled</code></td><td>Allow clients to opt out origin</td></tr><tr><td><code>dogstatsd_port</code></td><td>UDP listen port</td></tr><tr><td><code>dogstatsd_so_rcvbuf</code></td><td>Socket receive buffer size</td></tr><tr><td><code>dogstatsd_socket</code></td><td>UDS datagram socket path</td></tr><tr><td><code>dogstatsd_stream_log_too_big</code></td><td>Log oversized UDS stream frames</td></tr><tr><td><code>dogstatsd_stream_socket</code></td><td>UDS stream socket path</td></tr><tr><td><code>dogstatsd_string_interner_size</code></td><td>String interner capacity</td></tr><tr><td><code>dogstatsd_tag_cardinality</code></td><td>Default tag cardinality level</td></tr><tr><td><code>dogstatsd_tags</code></td><td>Extra tags added to all DSD data</td></tr><tr><td><code>enable_payloads.events</code></td><td>Allow sending event payloads</td></tr><tr><td><code>enable_payloads.series</code></td><td>Allow sending series payloads</td></tr><tr><td><code>enable_payloads.service_checks</code></td><td>Allow sending service check payloads</td></tr><tr><td><code>enable_payloads.sketches</code></td><td>Allow sending sketch payloads</td></tr><tr><td><code>expected_tags_duration</code></td><td>Host tag enrichment duration</td></tr><tr><td><code>extra_tags</code></td><td>Additional static tags</td></tr><tr><td><code>forwarder_backoff_base</code></td><td>Retry backoff base (secs)</td></tr><tr><td><code>forwarder_backoff_factor</code></td><td>Retry backoff jitter factor</td></tr><tr><td><code>forwarder_backoff_max</code></td><td>Retry backoff ceiling (secs)</td></tr><tr><td><code>forwarder_connection_reset_interval</code></td><td>HTTP conn reset interval (secs)</td></tr><tr><td><code>forwarder_num_workers</code></td><td>Concurrent forwarder workers</td></tr><tr><td><code>forwarder_recovery_interval</code></td><td>Backoff recovery decrease factor</td></tr><tr><td><code>forwarder_recovery_reset</code></td><td>Reset errors on success</td></tr><tr><td><code>forwarder_retry_queue_max_size</code></td><td>Retry queue max size (deprecated)</td></tr><tr><td><code>forwarder_retry_queue_payloads_max_size</code></td><td>Retry queue max size (bytes)</td></tr><tr><td><code>forwarder_storage_max_disk_ratio</code></td><td>Max disk usage ratio for retry</td></tr><tr><td><code>forwarder_storage_max_size_in_bytes</code></td><td>Max on-disk retry storage size</td></tr><tr><td><code>forwarder_storage_path</code></td><td>On-disk retry storage directory</td></tr><tr><td><code>forwarder_timeout</code></td><td>Forwarder HTTP request timeout</td></tr><tr><td><code>histogram_aggregates</code></td><td>Histogram aggregate statistics</td></tr><tr><td><code>histogram_copy_to_distribution</code></td><td>Copy histograms to distributions</td></tr><tr><td><code>histogram_copy_to_distribution_prefix</code></td><td>Prefix for hist-to-dist copies</td></tr><tr><td><code>histogram_percentiles</code></td><td>Histogram percentile quantiles</td></tr><tr><td><code>hostname</code></td><td>Configured hostname override</td></tr><tr><td><code>ipc_cert_file_path</code></td><td>IPC TLS certificate path</td></tr><tr><td><code>log_file</code></td><td>Log output file path</td></tr><tr><td><code>log_file_max_rolls</code></td><td>Max rotated log files kept</td></tr><tr><td><code>log_file_max_size</code></td><td>Max log file size before rotate</td></tr><tr><td><code>log_format_json</code></td><td>Use JSON log format</td></tr><tr><td><code>log_payloads</code></td><td>Debug-log decoded payload contents</td></tr><tr><td><code>log_to_console</code></td><td>Log to stdout/stderr</td></tr><tr><td><code>log_to_syslog</code></td><td>Log to syslog daemon</td></tr><tr><td><code>metric_filterlist</code></td><td>Metric name blocklist</td></tr><tr><td><code>metric_filterlist_match_prefix</code></td><td>Blocklist uses prefix matching</td></tr><tr><td><code>metric_tag_filterlist</code></td><td>Per-metric tag include/exclude</td></tr><tr><td><code>no_proxy_nonexact_match</code></td><td>Domain/CIDR <code>no_proxy</code> matching</td></tr><tr><td><code>observability_pipelines_worker.metrics.enabled</code></td><td>Route metrics to OPW instance</td></tr><tr><td><code>observability_pipelines_worker.metrics.url</code></td><td>OPW metrics intake URL</td></tr><tr><td><code>origin_detection_unified</code></td><td>Unified origin detection mode</td></tr><tr><td><code>provider_kind</code></td><td>Provider kind static tag</td></tr><tr><td><code>proxy</code></td><td>HTTP/HTTPS proxy configuration</td></tr><tr><td><code>run_path</code></td><td>Runtime data directory path</td></tr><tr><td><code>secret_backend_command</code></td><td>Secret resolver executable path</td></tr><tr><td><code>secret_backend_timeout</code></td><td>Secret backend timeout (seconds)</td></tr><tr><td><code>serializer_compressor_kind</code></td><td>Payload compression algorithm</td></tr><tr><td><code>site</code></td><td>Datadog site domain</td></tr><tr><td><code>statsd_metric_blocklist</code></td><td>Metric name blocklist</td></tr><tr><td><code>statsd_metric_blocklist_match_prefix</code></td><td>Blocklist uses prefix matching</td></tr><tr><td><code>statsd_metric_namespace</code></td><td>Prefix prepended to all metrics</td></tr><tr><td><code>statsd_metric_namespace_blacklist</code></td><td>Namespace prefixes exempt (alias)</td></tr><tr><td><code>syslog_rfc</code></td><td>Use RFC-style syslog header</td></tr><tr><td><code>syslog_uri</code></td><td>Syslog destination URI</td></tr><tr><td><code>tags</code></td><td>Global tags (DD_TAGS)</td></tr><tr><td><code>use_proxy_for_cloud_metadata</code></td><td>Proxy cloud metadata endpoints</td></tr><tr><td><code>use_v2_api.series</code></td><td>Send series via V2 protobuf endpoint</td></tr><tr><td><code>vector.metrics.enabled</code></td><td>Route metrics to OPW (legacy alias)</td></tr><tr><td><code>vector.metrics.url</code></td><td>OPW metrics intake URL (legacy alias)</td></tr></tbody></table>',78)])])}const _=e(r,[["render",s]]);export{p as __pageData,_ as default};
