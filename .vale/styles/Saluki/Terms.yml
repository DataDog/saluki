extends: substitution
message: Use '%s' instead of '%s'.
level: error
ignorecase: true
swap:
  datadog: Datadog
  dogstatsd: DogStatsD
  grpc: gRPC
  yaml: YAML
  json: JSON
  urls: URLs
  uris: URIs
  markdown: Markdown
