---
schema-version: v1
kind: mergequeue
merge_method: squash
---
schema-version: v2
kind: adms
auto-version-updates:
  disabled:
    reason: Existing dependency update/management tooling in place (Dependabot)
