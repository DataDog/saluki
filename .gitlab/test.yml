unit-tests:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make test

unit-tests-miri:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make test-miri

check-clippy:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make check-clippy

check-deny:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make check-deny

check-fmt:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make check-fmt

check-licenses:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make check-licenses

check-features:
  stage: test
  image: "${SALUKI_BUILD_CI_IMAGE}"
  script:
    - make check-features
