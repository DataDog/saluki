.windows-amd64-test-job:
  tags: ["windows-v2:2022"]
  interruptible: true
  variables:
    ARCH: "x64"
    # Mirrors the Datadog Agent Windows LTSC2022 build image.
    WINBUILDIMAGE: registry.ddbuild.io/ci/datadog-agent-buildimages/windows_ltsc2022_x64:latest
    OVERRIDE_GIT_STRATEGY: "clone"

unit-tests-windows-amd64:
  extends: .windows-amd64-test-job
  stage: test
  cache:
    key: windows-amd64-cargo
    paths:
      - .ci-cache/cargo/
      - .ci-cache/rustup/
  script:
    - $ErrorActionPreference = "Stop"
    - Write-Host "Cached Datadog Agent Windows build images before pull:"
    - docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}\t{{.Size}}" | Select-String "datadog-agent-buildimages/windows"
    - Write-Host "Cached LTSC2022 build image tags before pull:"
    - docker images registry.ddbuild.io/ci/datadog-agent-buildimages/windows_ltsc2022_x64 --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}\t{{.Size}}"
    - docker pull ${WINBUILDIMAGE}
    - >
      docker run --rm
      -m 24576M
      --storage-opt "size=50GB"
      -v "$(Get-Location):c:\mnt"
      -w c:\mnt
      -e CI=true
      -e GITLAB_CI
      -e CI_JOB_ID
      -e CI_PIPELINE_ID
      -e CI_COMMIT_SHA
      -e CI_PROJECT_NAME
      -e CI_COMMIT_REF_NAME
      -e WINDOWS_CI_CARGO_HOME="c:\mnt\.ci-cache\cargo"
      -e RUSTUP_HOME="c:\mnt\.ci-cache\rustup"
      -e CARGO_TARGET_DIR="c:\mnt\target\windows-ci"
      -e CARGO_NEXTEST_VERSION="0.9.99"
      -e WINDOWS_CI_PACKAGES="datadog-agent-commons,process-memory,saluki-error,ddsketch,prometheus-exposition,saluki-config"
      ${WINBUILDIMAGE}
      powershell.exe -NoProfile -NonInteractive -File c:\mnt\ci\tooling\windows-unit-tests.ps1
    - If ($lastExitCode -ne "0") { exit "$lastExitCode" }
